const vote = require('./actions/vote');
const options = require('./actions/options');
const choices = require('./actions/choices');
const core = require('./actions/core');

/* actions */

module.exports = { vote, options, choices, core };
